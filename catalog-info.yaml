apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: rails-api-template
  description: Production-ready Rails API with Grafana observability, authentication, and background jobs
spec:
  targets:
    - ./template.yaml
